---
name: Supplies Co brand resolution
description: How to resolve conflicting brand attachments for "THE SUPPLIES COMPANY LIMITED" document system, and the format-preservation constraint for this project.
---

The user supplies multiple AI-generated mockup images with inconsistent names ("THE SUPPLY CO., LTD.", navy/gold stationery mockups) alongside one explicit brand-guide image (title "ฟอนต์ตามภาพ และชื่อฟอนต์" / "TS Modern Tech"). When attachments conflict, the brand-guide image is authoritative for: company name ("THE SUPPLIES COMPANY LIMITED" — plural, with "COMPANY LIMITED" subtext line), logo icon (circle + left/right triangle arrows split by a center bar), and palette (Black #000000 / Light Gray #E5E5E5 / White #FFFFFF only). Ignore the navy/gold mockup images — those are unrelated stock stationery renders, not this brand.

Real contact/address details (street address, tax ID, phone) came from a separate quotation-template screenshot, not the brand guide — treat that image as the data source for boilerplate contact fields, not for branding/logo.

**Why:** The user explicitly said "ไม่เปลี่ยนรูปแบบ" (do not change the format) when asking for a full rebrand — meaning: keep existing document layouts/structure as-is, only correct the logo asset, wording, and enrich data content (departments, workflow, standards). Do not use a "rebrand" request as license to redo layouts from scratch.

**How to apply:** When conflicting logo/brand assets are attached in future requests for this project, prefer the one explicitly labeled as a brand/font guide over generic AI-mockup stationery images. Preserve the existing 3-palette-set template structure (Set A monochrome / B navy-gold / C navy-red) rather than collapsing to one style, since the user's format-preservation instruction overrides a strict "monochrome only" reading of an unrelated attachment.
